{
  "profile": "my-overlay",
  "note": "Private routing. Overwrites the public generic capabilities.json when this kit is stacked. Add your real providers here; skills ask for the capability, not the vendor (see the capability-routing skill).",
  "capabilities": {
    "warehouse": [
      { "provider": "cli", "command": "your-warehouse-cli", "about": "your data warehouse, reached via a host exec proxy (see host/overlay_example.go)" }
    ],
    "crm": [
      { "provider": "mcp", "server": "your-crm", "about": "your CRM MCP server (gateway catalog or local stdio)" }
    ]
  }
}
