{
  "name": "@pi-stack/memory",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "pi-stack memory store: facts + learnings, scored recall, local embeddings. No native deps (Node built-in node:sqlite).",
  "scripts": {
    "demo": "node cli.ts demo",
    "stats": "node cli.ts stats"
  }
}
